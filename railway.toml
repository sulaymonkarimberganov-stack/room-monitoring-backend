# Railway Configuration File
[build]
builder = "NIXPACKS"
buildCommand = "cd app && mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar app/target/app-0.0.1-SNAPSHOT.jar"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
