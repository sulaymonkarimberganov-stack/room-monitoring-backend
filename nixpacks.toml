[phases.setup]
nixPkgs = ["...", "maven", "jdk21"]

[phases.build]
cmds = [
  "mvn clean package -DskipTests"
]

[phases.start]
cmd = "java -jar target/app-0.0.1-SNAPSHOT.jar"

[variables]
MAVEN_OPTS = "-Xmx512m"
